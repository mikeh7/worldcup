// Central timings (ms) for the reveal sequence.
export const BALL_TOTAL_MS = 1600; // full sweep: left edge -> center -> off right
export const BALL_PEAK_MS = 800; // moment the ball fully obscures the screen
export const RUN_MS = 4500; // side-to-side running before stopping (<=10s)
export const BANNER_MS = 3800; // how long the welcome / winner banner stays
export const IDLE_MS = 20000; // no name entered for this long → idle showcase runs
